import * as React from 'react';
export interface WithGameProps {
    game: Phaser.Game;
}
declare const withGame: <T extends WithGameProps>(Component: React.ComponentType<T>) => {
    (props: Pick<T, Exclude<keyof T, "game">>): JSX.Element;
    displayName: string;
};
export default withGame;
