import * as React from 'react';
import { SpawnerContextValue } from './SpawnerContext';
export interface WithSpawnerProps {
    spawner: SpawnerContextValue;
}
declare const withSpawner: <T extends WithSpawnerProps>(Component: React.ComponentType<T>) => {
    (props: Pick<T, Exclude<keyof T, "spawner">>): JSX.Element;
    displayName: string;
};
export default withSpawner;
